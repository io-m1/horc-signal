# HORC Signal Framework Dependencies
# pip install -r requirements.txt

# Core (always required)
pytest>=7.0.0          # Testing framework

# Data Adapters (install based on your data source)
ib_insync>=0.9.86      # Interactive Brokers API (FREE with funded account)
polygon-api-client>=1.12.0  # Polygon.io (optional paid data)

# Optional - Enhanced functionality
pandas>=2.0.0          # DataFrame support for analysis
numpy>=1.24.0          # Numerical computing (used by pandas)

# Development (optional)
pytest-asyncio>=0.21.0 # Async test support for IB adapter tests
black>=23.0.0          # Code formatting
mypy>=1.0.0            # Type checking
